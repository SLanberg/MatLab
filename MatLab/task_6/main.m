% In MATLAB, functions must be defined in separate files with a .m 
% extension, and they must start with the function keyword and the 
% function definition statement. The code that calls the function must 
% be in a separate script

% Call function and display return value
disp(add_two)